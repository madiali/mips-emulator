## Skipped files

These are files that we're skipping for now and will come back to later, as time allows.

* `Sound.cs`
* `SoundModule.cs`
* `SoundWaveGenerator.cs`
* `ProgramLoader.cs`
  * Uses `Mips.cs`, so `Mips.cs` (along with whatever `Mips.cs` uses) probably needs to be done first.